# KannadaBuddy runs on port 8001 only. Your existing nginx config is correct:
#   location / { proxy_pass http://127.0.0.1:8001; ... }
# No extra location blocks needed. If /auth/google returns 404, the app
# on 8001 is an old build — deploy the current server/ (app.py with auth, db.py) and restart.
